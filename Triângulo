programa {
  funcao inicio() {
    inteiro base
    inteiro altura

    escreva("Qual o tamanho da base?")
    leia(base)

    escreva("Qual o tamanho da altura?")
    leia(altura)

    inteiro area = (altura * base) / 2

    escreva("A área é de ", area)
    
  }
}
